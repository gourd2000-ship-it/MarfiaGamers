# 개발 로드맵: 웹앱 + 실시간 서버 마피아 게임

## 목적과 원칙

이 로드맵은 [PRD](./prd.md)의 웹·서버 capability map을 의존성 순서로 나눈 것이다. 개발은 **구조 → 공통 기능 → 개별 기능** 순서를 지키며, 서버 권한의 게임 규칙과 학교 태블릿 브라우저 접속을 먼저 검증한다. 이전 Android LAN 로드맵은 더 이상 사용하지 않는다.

## 현재 구현 상태 (2026-09-03)

- **Phase 1 구조:** 완료 — 공유 계약, Socket 서버, React 웹앱, 테스트·lint·빌드 명령이 준비되어 있다.
- **Phase 2 공통 기능:** 코드 완료 — QR 초대, 로비, 시작 후 입장 차단, 서버 역할 배정·타이머, 자동 기권, 공개/개인 상태 분리를 구현했다. 학교 기기 HTTPS/WSS 실기기 검증은 남아 있다.
- **Phase 3 개별 기능:** 낮 비공개 투표·재투표, 마피아/의사/경찰 행동, 승패 결과·재경기·방 종료를 구현했다. QR 링크를 사용한 두 브라우저 Playwright E2E도 통과했다. 실제 학교 기기 E2E는 남아 있다.

## 기술 스택 적용 원칙

- **React + TypeScript**는 학생·방장이 사용하는 웹 화면만 담당한다. 역할·투표·타이머의 최종 판단을 브라우저에 두지 않는다.
- **Node.js + Socket.IO**는 HTTPS 입장과 WSS 실시간 통신을 담당하며, 연결한 참가자에게 공개 상태 또는 허용된 개인 이벤트만 전송한다.
- **서버 게임 엔진**은 순수 TypeScript 모듈로 작성해 UI·Socket 코드와 분리한다. 이 때문에 규칙은 서버 단위 테스트로, 화면은 브라우저 테스트로 각각 검증할 수 있다.
- **서버 메모리**는 MVP의 방 상태만 보관한다. 데이터베이스·다중 서버 확장은 서버 재시작 후 복구가 필요해질 때 별도 승인으로 도입한다.
- Phase 0에서는 이 스택의 전제인 학교망 HTTPS/WSS 접근을 검증한다. 통과 전에는 역할 화면이나 LAN 대체 기능을 구현하지 않는다.

## 의존성 그래프

```text
공유 계약·프로젝트 구조
 ├─ 웹 앱 골격 ────────────┐
 └─ 실시간 게이트웨이      │
      └─ 방 세션·자동 기권  │
          └─ 서버 게임 엔진 ─┼─ 공통 게임 화면·투표
                              │      ├─ 마피아 역할
                              │      ├─ 의사 역할
                              │      └─ 경찰 역할
                              └─ 결과·재경기·기록
```

## Phase 0 — 사양·학교망 검증 (구조 이전 게이트)

### Task 0.1: 학교 태블릿 웹 접속 가능성 검증

**설명:** HTTP 웹 접속 가능 여부와 별도로, 배포 후보의 HTTPS 페이지·QR 카메라·WSS Socket 연결을 학교 태블릿에서 열어 앱 설치 없이 게임 서버에 연결할 수 있는지 확인한다.

**수용 기준:**

- [ ] 학교 태블릿 Chrome/Edge에서 HTTPS 테스트 페이지와 QR URL이 열린다.
- [ ] 두 브라우저에서 WSS 연결·이벤트 왕복이 성공한다.
- [ ] QR 카메라 권한이 가능하며, 거부 시 URL 입력 대안이 동작한다.
- [ ] 차단 시 필요한 도메인 허용 목록과 담당 부서를 기록한다.

**검증:** 학교망 태블릿 2대와 외부망 1대에서 연결을 10회씩 시도하고 성공률·오류를 기록한다.

**의존성:** 없음
**예상 범위:** S

### Gate: 사양 승인

- [ ] PRD의 capability map과 Open Questions를 검토한다.
- [ ] 호스팅 방식, 도메인, 단일 서버 MVP 운영 책임자를 정한다.
- [ ] 서버 히스토리 미저장, 자동 기권, 의사의 게임당 1회 자기 보호, 비공개 투표·득표 수 공개를 확정한다.

---

## Phase 1 — 구조

### Task 1.1: TypeScript 모노레포와 품질 기준

**설명:** `apps/web`, `apps/server`, `packages/contracts` 구조와 공통 TypeScript, lint, 단위 테스트, 빌드 명령을 만든다.

**수용 기준:**

- [ ] 웹 앱과 서버가 독립적으로 개발 실행·빌드된다.
- [ ] 공유 계약 패키지는 웹과 서버에서 같은 타입·스키마를 제공한다.
- [ ] `npm run test`, `npm run lint`, `npm run build`가 빈 골격에서 통과한다.

**검증:** 각 기본 명령과 브라우저에서 웹 앱·상태 페이지 수동 확인.
**의존성:** Task 0.1
**예상 범위:** M

### Task 1.2: 이벤트 계약과 서버 연결 수직 슬라이스

**설명:** 방 입장에 필요한 HTTP/Socket 이벤트, 입력 스키마, 서버 세션 식별자를 정의하고 브라우저에서 연결 상태를 표시한다.

**수용 기준:**

- [ ] 잘못된 payload와 허용되지 않은 이벤트는 서버에서 거부된다.
- [ ] 브라우저는 연결·재연결·오류 상태를 표시한다.
- [ ] 이벤트 타입이 웹·서버에서 중복 정의되지 않는다.

**검증:** Socket 통합 테스트와 브라우저 연결/재연결 수동 테스트.
**의존성:** Task 1.1
**예상 범위:** M

### Checkpoint: 구조

- [ ] 학교 태블릿에서 웹앱과 WSS 연결을 다시 확인한다.
- [ ] 공유 계약, 코드 경계, 비밀 환경 변수 처리 방식을 리뷰한다.
- [ ] 테스트·lint·빌드가 모두 통과한다.

---

## Phase 2 — 공통 기능

### Task 2.1: 방 URL·QR·로비·자동 기권

**설명:** 방 생성, 초대 URL/QR, 닉네임 입장, 참가자 목록, 준비 상태, 인원별 표준 역할 프리셋과 자동 기권을 하나의 완료된 흐름으로 구현한다.

**수용 기준:**

- [ ] 계정 없이 QR/URL로 4~20명 방에 입장할 수 있다.
- [ ] 방장만 방 이름·최대 인원·타이머를 설정하고 시작·종료할 수 있다. 역할은 시스템 프리셋으로 결정한다.
- [ ] 연결이 끊긴 참가자는 자동 기권 처리되고 복귀할 수 없다.

**검증:** 두 브라우저와 다중 Socket 클라이언트 통합 테스트, 학교 태블릿 QR·자동 기권 수동 테스트.
**의존성:** Task 1.2
**예상 범위:** M

### Task 2.2: 서버 권한 게임 엔진과 타이머

**설명:** 순수 서버 상태 기계로 역할 배정, 상태 전이, 서버 기준 타이머, 생존자·승패 판정을 구현한다.

**수용 기준:**

- [ ] 브라우저가 임의 상태·타이머·역할을 확정할 수 없다.
- [ ] 유효한 명령에서만 정의된 상태 전이가 일어난다.
- [ ] 역할 구성·인원·승패 조건이 서버에서 검증된다.
- [ ] 방장은 방 이름·최대 인원·타이머를 설정하고 시작만 할 수 있으며, 서버가 표준 프리셋 역할 배정과 이후 게임 진행을 자동 처리한다.
- [ ] 인원별 표준 프리셋, 의사의 게임당 1회 자기 보호, 자동 기권의 승패 판정이 엔진에서 처리된다.

**검증:** 4·5·6·8·12인 조합, 역할이 없는 밤 단계 건너뛰기, 시간 만료, 승패 경계의 엔진 단위 테스트.
**의존성:** Task 1.2, Task 2.1
**예상 범위:** M

### Task 2.3: 공개 상태·개인 이벤트·공통 진행 UI

**설명:** 공개 상태 방송과 수신자별 개인 이벤트를 분리하고, 역할 공개·상태 안내·타이머·연결 상태 화면을 구현한다.

**수용 기준:**

- [ ] 공개 payload에는 역할·행동·조사 결과가 포함되지 않는다.
- [ ] 역할 정보는 해당 참가자 한 명에게만 전달된다.
- [ ] 자동 기권한 참가자에게는 개인 상태를 다시 전송하지 않는다.

**검증:** Socket 수신자 검사와 다중 브라우저 통합 테스트.
**의존성:** Task 2.2
**예상 범위:** M

### Checkpoint: 공통 기능

- [ ] 학생 4·6명이 URL로 입장해 역할 카드를 받는 흐름이 인터넷을 통해 동작한다.
- [ ] 서버 로그와 테스트에서 개인 역할 정보 유출이 없다.
- [ ] 연결 단절 뒤 자동 기권과 공개 안내를 확인한다.

---

## Phase 3 — 개별 기능

### Task 3.1: 낮 진행·지목·투표

**설명:** 아침 결과, 토론·지목·투표 타이머, 생존자 투표, 동률 재투표, 탈락 공지를 구현한다.

**수용 기준:**

- [ ] 생존자만 한 번 투표하며 마감 후에는 투표할 수 없다.
- [ ] 동률은 한 번 재투표하고 재동률이면 탈락 없음으로 처리된다.
- [ ] 누가 누구에게 투표했는지는 비공개이고, 종료 뒤 후보별 득표 수와 결과만 모든 브라우저에 같은 `revision`으로 표시된다.

**검증:** 투표·동률 엔진 테스트와 3개 브라우저 Playwright 시나리오.
**의존성:** Task 2.3
**예상 범위:** M

### Task 3.2: 마피아 밤 행동

**설명:** 마피아에게만 대상 선택을 제공하고 내부 다수결을 서버에서 처리한다.

**수용 기준:**

- [ ] 마피아가 아닌 참가자는 선택 UI·대상·득표를 수신하지 않는다.
- [ ] 동률·미제출·다수결이 PRD 규칙대로 처리된다.
- [ ] 낮에는 필요한 사망 결과만 공개된다.

**검증:** 권한 누출 검사와 마피아 1·2·3명 Socket 통합 테스트.
**의존성:** Task 2.3, Task 3.1
**예상 범위:** M

### Task 3.3: 의사 밤 행동

**설명:** 의사 개인 보호 선택과 마피아 결과의 서버 결합 규칙을 구현한다.

**수용 기준:**

- [ ] 의사만 보호 대상을 선택할 수 있다.
- [ ] 보호·미선택·설정된 자기/연속 보호 규칙이 정확히 적용된다.
- [ ] 보호 대상·성공 여부는 공개되지 않는다.

**검증:** 제거·보호 같은 대상/다른 대상/미선택 엔진·Socket 테스트.
**의존성:** Task 3.2
**예상 범위:** S

### Task 3.4: 경찰 밤 행동

**설명:** 경찰 개인 조사와 비공개 결과 전송을 구현한다.

**수용 기준:**

- [ ] 경찰만 조사 결과를 수신한다.
- [ ] 결과는 대상의 실제 진영과 일치한다.
- [ ] 사망·미선택·시간 만료가 안전하게 처리된다.

**검증:** 모든 조사 대상과 수신자 권한을 검사하는 테스트.
**의존성:** Task 3.2
**예상 범위:** S

### Task 3.5: 결과·재경기·운영 상태

**설명:** 승리 진영·역할 공개·재경기와 최소 서버 상태 확인을 구현한다.

**수용 기준:**

- [ ] 게임 종료 시 역할·승리 진영을 표시하고, 같은 참가자로 새 게임을 시작할 수 있다.
- [ ] 방 종료 시 서버 메모리의 방·토큰이 삭제된다.
- [ ] 운영자는 비밀 게임 데이터 없이 서버 생존·연결 오류를 확인할 수 있다.

**검증:** 마피아/시민 승리 E2E, 자동 기권 뒤 복귀 거부, 방 종료 뒤 세션 거부, 상태 확인 수동 테스트.
**의존성:** Task 3.3, Task 3.4
**예상 범위:** M

### Checkpoint: MVP 완료

- [ ] 4~20명 설정 범위에서 URL 입장 → 밤 → 낮 → 승패 → 재경기 E2E 시나리오가 통과한다.
- [ ] 학교 태블릿에서 HTTPS, QR, WSS, 자동 기권, QR 권한 거부 대안을 확인한다.
- [ ] PRD의 모든 MVP 성공 기준과 개인정보 삭제 기준을 검토한다.

## 위험 요소와 완화

| 위험 | 영향 | 완화 |
|---|---|---|
| 학교망이 배포 도메인 또는 WSS를 차단 | 높음 | Phase 0에서 실기기 검증하고, IT 허용 목록 요청과 오류 안내를 준비한다. |
| 무료 호스팅의 절전·연결 제한 | 높음 | 항상 실행되는 호스팅을 선택하고, MVP 단일 인스턴스 제약을 명시한다. |
| 서버 장애로 진행 중 방 손실 | 중간 | MVP는 명시적 종료 안내, 후속 버전에서 영속 저장·복구를 검토한다. |
| 역할·토큰 유출 | 높음 | 서버 권한 엔진, 수신자별 이벤트, HTTPS/WSS, 로그 마스킹과 테스트를 적용한다. |
| 초대 URL의 무단 공유 | 중간 | 고엔트로피 토큰, 시작 후 입장 차단, 방장 종료를 적용한다. |

## 병렬화 원칙

- 순차 진행: Task 0.1~2.3은 공통 계약·세션·서버 상태를 바꾸므로 순차 진행한다.
- 병렬 가능: Task 3.2~3.4는 Task 2.3의 개인 이벤트 계약이 고정된 뒤 병렬 구현할 수 있다.
- 조율 필요: 웹 UI와 서버를 병렬 구현할 때는 `packages/contracts`의 이벤트 스키마 변경을 먼저 합의한다.
