# PRD: 웹 기반 마피아 게임 진행 앱

## 1. 문서 상태와 전환 가정

- 상태: **검토용 초안**. 이 문서는 이전 Android LAN 앱 계획을 대체한다. 구현은 이 PRD와 `roadmap.md` 승인 뒤 시작한다.
- 대상: 학교 관리 태블릿 또는 PC 브라우저에서 참여하는 학생 4~20명.
- 클라이언트: 설치 없는 반응형 웹앱(PWA 선택 지원). 최신 Chrome·Edge·Safari를 지원 대상으로 한다.
- 연결: 참가자는 같은 Wi-Fi일 필요 없이 인터넷으로 접속한다. 학교망에서 일반 HTTP 웹 접속은 가능한 것으로 확인됐지만, QR 카메라와 안전한 실시간 통신을 위해 HTTPS/WSS 허용은 Phase 0에서 별도로 검증한다. 외부의 권한 있는 실시간 서버가 방·역할·타이머·투표 상태를 처리한다.
- 입장: 방장이 생성한 HTTPS 초대 URL 또는 QR 코드로 참여한다. 계정·전화번호·앱 설치는 요구하지 않는다.
- 운영: MVP는 항상 실행되는 단일 서버 인스턴스가 방 상태를 메모리에 보관한다. 서버 재시작 시 진행 중인 방은 복구되지 않는다.
- MVP 역할: 시민, 마피아, 의사, 경찰. 특수 역할·관전·음성 채팅은 후속 범위다.

## 2. Objective

학생들이 학교 태블릿의 브라우저에서 QR 코드를 열고 1분 안에 방에 입장해, 사회자 없이 마피아 게임 한 판을 진행할 수 있게 한다. 학교 기기 관리 정책, 개발자 옵션, 기기 간 로컬 통신 권한에 의존하지 않는다.

### 성공 사용자 흐름

1. 방장이 웹앱에서 `방 만들기`를 누른다.
2. 앱은 초대 URL과 QR 코드를 보여 준다.
3. 참가자는 QR을 스캔하거나 URL을 열고 닉네임만 입력해 로비에 참여한다.
4. 방장이 인원과 역할 구성을 정하고 게임을 시작한다.
5. 서버가 개인 역할·밤 행동·낮 진행·타이머·투표·승패를 권한 있게 처리한다.
6. 결과를 확인하고 같은 참가자로 재경기하거나 방을 닫는다.

### 측정 가능한 성공 기준

- 관리 태블릿에서 앱 설치나 개발자 옵션 없이 URL/QR로 참여할 수 있다.
- 일반적인 인터넷 환경에서 초대 URL을 연 참가자는 30초 안에 로비에 입장한다.
- 4~20명 방에서 연결된 참가자는 상태 전환을 2초 이내에 받는다.
- 서버는 역할·밤 행동·경찰 조사 결과를 권한 없는 브라우저에 보내지 않는다.
- 서버 재시작·방 종료 뒤에는 방의 개인 게임 데이터와 초대·세션 토큰을 폐기한다.
- WebSocket 사용이 제한된 학교망에서는 명확한 연결 오류와 재시도 안내를 보여 준다.

## 3. 범위

### MVP에 포함

- HTTPS 초대 URL·QR 코드, 닉네임 기반 로비 참여
- 방장 설정, 준비 상태, 인원별 표준 역할 프리셋, 타이머 검증
- 서버 권한의 역할 무작위 배정과 개인 역할 카드
- 밤 순서: 마피아 제거 대상 다수결 → 의사 보호 → 경찰 조사
- 낮 안내, 토론·지목·투표 타이머, 동률 재투표, 탈락·승패 판정
- 실시간 공개 상태 동기화, 개인 이벤트, 연결 끊김 즉시 자동 기권 처리
- 결과·재경기 및 선택적인 브라우저 내 최근 게임 기록
- 운영자용 기본 상태 확인(서버 가동 여부, 오류 로그)

### MVP에서 제외

- 계정·친구 목록·개인 프로필·결제·광고
- 네이티브 앱 배포, mDNS/Bonjour, 로컬 Wi-Fi 방 발견, Android 개발자 옵션
- 음성·영상 채팅, AI 음성 사회자, 채팅 기록
- 특수 역할, 사용자 제작 규칙, 관전자
- 진행 중 방의 영구 저장·다중 서버 확장·서버 장애 후 방 복구

## 4. Capability Map (검토 게이트)

| Module ID | 책임 | 의존성 |
|---|---|---|
| `shared-contracts` | 도메인 모델, 입력 스키마, Socket 이벤트 타입 | — |
| `web-shell` | 브라우저 진입, 라우팅, QR, 접근성, 공통 UI | `shared-contracts` |
| `realtime-gateway` | HTTPS 입장 API, Socket 연결, 속도 제한, 세션 인증 | `shared-contracts` |
| `room-session` | 방 코드, 참가자, 방장 권한, 자동 기권, 로비 | `realtime-gateway` |
| `game-engine` | 서버 권한 상태 기계, 역할 배정, 타이머, 승패 | `room-session` |
| `game-flow` | 로비·역할·밤·낮·투표 화면과 실시간 상태 표시 | `game-engine`, `web-shell` |
| `game-roles` | 마피아·의사·경찰의 개인 행동과 결과 화면 | `game-flow` |
| `results-history` | 결과·재경기·브라우저 내 최소 기록 | `game-engine`, `game-flow` |

빌드 순서: `shared-contracts` → `web-shell`, `realtime-gateway` → `room-session` → `game-engine` → `game-flow` → `game-roles` → `results-history`.

`realtime-gateway`와 `game-engine`은 반드시 서버에서 동작한다. 브라우저는 명령을 제안할 수 있을 뿐, 게임 상태·역할·타이머를 확정할 수 없다.

## 5. 핵심 기능 요구사항

### 5.1 방 생성·입장

- 방 생성자는 방 이름, 최대 인원과 타이머를 정한다. 역할은 참가 인원에 맞는 표준 프리셋으로 시스템이 결정한다.
- 서버는 추측하기 어려운 `room_id`와 초대 토큰을 포함한 HTTPS URL을 만들고, 해당 URL을 QR 코드로 표시한다.
- 참가자는 URL을 연 뒤 1~12자 닉네임을 제출해 입장한다. 입장 전에는 계정이 필요 없다.
- 게임 시작 뒤에는 신규 입장을 막는다. MVP에서는 연결이 끊겨 자동 기권한 참가자의 복귀를 허용하지 않는다.
- 방장은 로비에서만 설정 변경·게임 시작·방 종료를 할 수 있다.
- 방장은 특정 참가자에게 역할을 직접 지정하거나, 게임 중 단계·투표·승패 결과를 수동으로 바꿀 수 없다.

### 5.2 실시간 상태·권한

단일 진실 원천은 서버의 `game-engine`이다. 브라우저의 Socket 명령은 서버가 세션, 권한, 단계, 입력값, 중복 여부를 검증한 뒤에만 반영한다.

상태 전이:

`lobby → role-reveal → night-mafia → night-doctor → night-police → day-briefing → day-discussion → day-nomination → day-vote → (night-mafia | game-over)`

- 서버는 공개 상태를 방 전체에 방송하고, 역할·밤 행동·조사 결과는 지정된 참가자에게만 전송한다.
- 서버 시간이 타이머의 기준이며 브라우저 타이머는 표시용이다.
- 각 참가자는 단계별 행동을 한 번만 제출할 수 있다. 네트워크 재시도는 같은 명령 ID로 멱등 처리한다.
- 방장이 `게임 시작`을 누르면 서버가 역할을 자동 무작위 배정하고, 이후 밤·낮 전환, 시간 종료, 행동 미제출 처리, 투표 집계, 탈락, 승패 판정까지 자동으로 진행한다.

### 5.3 MVP 역할·투표 규칙

| 역할 | 밤 행동 | 비공개 결과 |
|---|---|---|
| 마피아 | 살아 있는 시민 측 한 명을 내부 다수결로 선택 | 마피아 선택 UI만 마피아에게 전송 |
| 의사 | 살아 있는 한 명을 보호 | 보호 대상과 성공 여부를 공개하지 않음 |
| 경찰 | 살아 있는 한 명을 조사 | 대상의 마피아 여부를 경찰에게만 전송 |
| 시민 | 행동 없음 | 낮 토론·지목·투표 참여 |

- 역할 분배는 서버의 암호학적으로 안전한 난수를 사용한다. 방장은 역할별 인원 수·배정 대상·결과를 선택할 수 없다.
- MVP는 아래 `표준` 프리셋만 사용한다. 실제 수업 플레이 테스트 후 수치를 조정할 수 있으나, 사용자 지정 역할 조합은 후속 범위다.

| 참가자 수 | 마피아 | 의사 | 경찰 | 시민 |
|---|---:|---:|---:|---:|
| 4명 | 1 | 0 | 0 | 3 |
| 5명 | 1 | 0 | 1 | 3 |
| 6명 | 1 | 1 | 1 | 3 |
| 7명 | 2 | 1 | 1 | 3 |
| 8~9명 | 2 | 1 | 1 | 나머지 |
| 10~13명 | 3 | 1 | 1 | 나머지 |
| 14~17명 | 4 | 1 | 1 | 나머지 |
| 18~20명 | 5 | 1 | 1 | 나머지 |

- 마피아 투표가 동률이거나 미제출이면 아무도 제거하지 않는다.
- 낮 투표는 생존자만 한 표를 행사한다. 누가 누구에게 투표했는지는 비공개이며, 투표 종료 후 후보별 득표 수와 최종 탈락자만 공개한다. 동률이면 한 번 재투표하고 재동률이면 아무도 탈락하지 않는다.
- 의사는 게임 전체에서 한 번만 자신을 보호할 수 있다. 다른 참가자 보호와 연속 보호는 제한하지 않는다.
- 특정 프리셋에 없는 역할의 밤 단계(예: 4명의 의사·경찰 단계)는 서버가 자동으로 건너뛴다.

### 5.4 자동 기권·오류 처리

- Socket 연결이 끊긴 참가자는 서버가 즉시 `자동 기권` 처리한다. 기권자는 사망자와 같이 이후 행동·지목·투표에서 제외되며, 승패 판정의 생존자 수에도 포함되지 않는다.
- 자동 기권은 공개적으로 안내하지만 역할·밤 행동은 공개하지 않는다. MVP에서는 기권 뒤 재접속·복귀를 허용하지 않는다.
- 서버 재시작, 초대 토큰 만료, 방 종료는 복구할 수 없음을 명확히 안내하고 새 방 만들기 경로를 제공한다.
- 브라우저가 QR 카메라 권한을 거부하면 URL 붙여넣기와 방 코드 입력 대안을 제공한다.

## 6. 비기능 요구사항

- **보안:** HTTPS/WSS만 허용한다. 초대·세션 토큰은 충분한 엔트로피를 가진 단기 값으로 발급하고, 서버 로그·분석 도구·URL 공유 미리보기에 비밀값을 기록하지 않는다.
- **개인정보:** 계정·전화번호·위치·연락처를 수집하지 않는다. 서버는 진행 중인 방에 필요한 닉네임·세션 데이터만 메모리에 유지하고 방 종료 시 삭제한다.
- **학교 환경:** 앱은 설치·USB 디버깅·로컬 네트워크 검색을 요구하지 않는다. HTTP 웹 접속 가능 여부와 별개로, 배포 도메인의 HTTPS, QR 카메라 권한, WSS 연결이 학교 필터에서 허용되는지 사전 확인한다.
- **접근성:** 큰 터치 대상, 고대비, 색상 외의 상태 표시, 한국어 화면 낭독 레이블, 키보드 조작을 지원한다.
- **가용성:** MVP는 단일 서버 인스턴스다. 모니터링은 서버 상태와 오류율을 확인하며, 무중단·자동 확장은 후속 범위다.

## 7. 기술 스택 및 프로젝트 구조

TypeScript를 웹과 서버에 공통으로 사용해 초보자의 언어 전환 부담을 낮춘다. React는 브라우저 UI와 TypeScript를 지원하고, Socket.IO는 브라우저와 Node.js 서버 간 양방향 이벤트·확인 응답을 제공한다. 정확한 의존성 버전은 프로젝트 초기화 시 lockfile에 고정한다.

| 구성 | 선택 | 책임 |
|---|---|---|
| 웹 클라이언트 | React + TypeScript | 반응형 로비·게임 화면, QR, 접근성, 서버 상태 표시 |
| 실시간 서버 | Node.js + TypeScript + Socket.IO | HTTPS 입장, Socket 세션, 방 방송, 자동 기권 처리 |
| 게임 규칙 | 서버 측 순수 TypeScript 모듈 | 역할 배정, 단계 전이, 타이머, 투표·승패 판정 |
| 입력 검증 | 공유 런타임 스키마 | HTTP·Socket payload 검증, 타입 계약 |
| 임시 상태 | 서버 메모리 | MVP 방·참가자·게임 상태. 재시작 시 폐기 |
| 배포 | 정적 웹 호스팅 + 항상 실행되는 Node.js 호스팅 | HTTPS 웹앱과 WSS 실시간 서버 공개 |
| 테스트 | Vitest, React Testing Library, Socket 통합 테스트, Playwright | 규칙·화면·다중 브라우저 E2E 검증 |

### 기술 스택 동작 방식

```text
학교 태블릿 브라우저
        ↕ HTTPS / WSS
React + TypeScript 웹앱
        ↕ Socket 이벤트
Node.js + Socket.IO 서버
        ↕
서버 권한 게임 엔진 · 방 메모리 상태
```

- **React + TypeScript:** 학생과 방장이 브라우저에서 사용하는 UI를 제공한다. URL만 열면 되므로 앱 설치나 개발자 옵션이 필요 없고, TypeScript가 화면·게임 데이터 형식의 불일치를 미리 찾는다.
- **Node.js + Socket.IO:** 서버가 방 입장, 상태 방송, 자동 기권과 양방향 실시간 이벤트를 처리한다. 서버가 응답을 확인하고 연결 상태를 관리하므로, 참가자별 게임 화면을 즉시 갱신할 수 있다.
- **서버 측 게임 엔진:** 역할 배정, 타이머 종료, 투표 집계, 승패 판정을 서버에서만 확정한다. 브라우저는 화면 표시와 명령 제출만 담당한다.
- **공유 계약:** 웹과 서버가 같은 이벤트 이름·데이터 스키마를 사용한다. 한쪽만 수정되어 입장·투표·역할 이벤트가 깨지는 문제를 줄인다.
- **서버 메모리:** MVP는 개인정보 보관을 최소화하기 위해 방 데이터를 영구 데이터베이스에 저장하지 않는다. 대신 서버 재시작 시 진행 중인 방을 복구할 수 없다.
- **정적 웹 호스팅 + 서버 호스팅:** 웹 화면은 정적 파일로 배포하고, WSS 연결을 유지할 수 있는 별도 Node.js 서버를 항상 실행한다. 학교에서는 이 두 주소에 대한 HTTPS/WSS 접근만 허용되면 된다.

공식 근거: [React의 TypeScript 안내](https://react.dev/learn/typescript), [Socket.IO 프로토콜 문서](https://github.com/socketio/socket.io/blob/main/docs/socket.io-protocol/v5-current.md), [Node.js TypeScript 문서](https://nodejs.org/api/typescript.html).

```text
apps/
  web/                         # React 브라우저 앱
    src/features/              # 로비, 역할, 밤, 낮, 투표, 결과 화면
    src/shared/                # 라우팅, Socket 클라이언트, 공통 UI
  server/                      # Node.js 권한 서버
    src/gateway/               # HTTP, Socket 인증·방 방송
    src/session/               # 방·참가자·자동 기권 세션
    src/game/                  # 순수 게임 엔진·역할 규칙
packages/
  contracts/                   # 공유 타입·입력 스키마·이벤트 이름
tests/
  integration/                 # 서버·Socket 다중 클라이언트 테스트
  e2e/                         # Playwright 브라우저 시나리오
docs/                          # 배포, 운영, 학교망 점검 안내
```

프로젝트 초기화 후 기본 검증 명령은 다음으로 통일한다.

```powershell
npm run dev
npm run test
npm run lint
npm run build
npm run test:e2e
```

## 8. 코드 스타일

- 게임 규칙은 React·Socket.IO·HTTP와 분리된 순수 TypeScript 상태 기계로 작성한다.
- 타입·컴포넌트는 `UpperCamelCase`, 변수·함수는 `lowerCamelCase`, 전송 이벤트 이름은 `kebab-case`를 사용한다.
- 외부 입력은 공유 스키마에서 검증하고, 서버의 상태 전이는 명시적 명령으로만 일으킨다.

```ts
function applyCommand(state: GameState, command: GameCommand): GameState {
  if (!state.canAccept(command)) {
    return state;
  }

  return state.transition(command);
}
```

## 9. 테스트 전략

- `game-engine`: Vitest로 역할 조합, 투표 동률, 시간 만료, 승패 경계, 권한 검증을 단위 테스트한다. 상태 전이·권한 분기는 100% 커버리지를 목표로 한다.
- `realtime-gateway`: 여러 Socket 클라이언트로 손상 payload, 다른 방 이벤트, 중복 명령, 자동 기권, 개인 이벤트 수신자를 통합 테스트한다.
- `web-shell`과 `game-flow`: React Testing Library로 화면 상태·접근성을 테스트한다.
- `e2e`: Playwright로 서로 다른 브라우저 컨텍스트의 방 생성 → QR/URL 입장 → 게임 종료 흐름을 검증한다.
- 실제 학교 태블릿에서 URL 접근, QR 카메라 권한, WSS 연결, 네트워크 단절 뒤 자동 기권을 수동 검증한다.

## 10. 경계

- **Always:** 서버에서 모든 권한·입력·상태 전이를 검증하고, HTTPS/WSS와 비밀 환경 변수를 사용하며, 변경마다 관련 테스트를 실행한다.
- **Ask first:** 호스팅 업체·도메인·비용 결정, 데이터베이스·계정·분석 도입, 서버 확장·영속화, 역할 규칙 변경, 학교 도메인 허용 목록 요청.
- **Never:** 역할·행동·토큰을 클라이언트 신뢰값으로 취급하거나, 비밀값을 소스·로그·브라우저 공개 상태에 기록하거나, 실패한 테스트를 삭제해 통과시키지 않는다.

## 11. Open Questions

1. 학교 태블릿에서 외부 HTTPS 사이트와 WebSocket(WSS)에 접속할 수 있나요? HTTP 접속 가능과 별도로 도메인 허용 목록 요청이 필요한가요?
2. MVP 서버 운영 비용과 도메인 관리는 누가 맡나요? 무료 티어의 자동 절전은 허용할 수 있나요?
3. 게임 히스토리는 서버에 저장하지 않는다. 브라우저 내 최근 게임 기록도 MVP에서 제외할까요, 기기에서만 제공할까요?
4. 방장이 브라우저를 닫으면 즉시 방을 종료할까요, 다른 참가자에게 방장 권한을 넘길까요?
