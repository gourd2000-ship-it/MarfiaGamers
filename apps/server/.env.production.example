# The port exposed only on the mini PC. A tunnel publishes it over HTTPS.
PORT=3000

# The production Vercel URL allowed to connect to Socket.IO.
# Multiple Vercel domains can be comma-separated when a preview deployment is needed.
WEB_ORIGIN=https://your-project.vercel.app
