# Bind the Node.js server only to the mini PC. Caddy publishes HTTPS/WSS.
HOST=127.0.0.1
PORT=3000

# Required in production. Set this to the exact public Caddy hostname.
# Multiple origins can be comma-separated only when they are all intentional.
WEB_ORIGIN=https://marfia-class.duckdns.org
